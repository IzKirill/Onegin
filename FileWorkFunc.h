#ifndef __FILEWORKFUNC_H__
#define __FILEWORKFUNC_H__

#define MAX_COUNT_CHARS 250

char* ScanFile (char filename[], MassiveStings* MS);
int FormsMassiveStrigns (char filename[], MassiveStings* MS);

#endif
